import ProjectDescription

public enum DesignSystem {
    public static let target = Target.moduleTarget(
        name: "DesignSystem",
        hasResources: true,
        dependencies: [
        ]
    )
}
